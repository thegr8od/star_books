server:
  port: 9090
#  forward-headers-strategy: framework
livekit:
  api:
    key: ${LIVEKIT_REMOVED:devkey}
    secret: ${LIVEKIT_API_SECRET:secret}

spring:
  config:
    activate:
      on-profile: prod
  datasource:
    url: ${DB_URL}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver

  hikari:
    maximum-pool-size: 10
    minimum-idle: 2
    idle-timeout: 30000
    max-lifetime: 1800000

  jpa:
    open-in-view: true
    hibernate:
      ddl-auto: create
      naming:
        physical-strategy: org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
    show-sql: true
    properties:
      hibernate.format_sql: true
      dialect: org.hibernate.dialect.MySQL8InnoDBDialect
      hibernate.id.new_generator_mappings: false

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email
            redirect-uri: "https://i12d206.p.ssafy.io/login/oauth2/code/google"
            authorization-grant-type: authorization_code
          naver:
            client-id: ${NAVER_CLIENT_ID}
            client-secret: ${NAVER_CLIENT_SECRET}
            scope:
              - name
              - email
            redirect-uri: "https://i12d206.p.ssafy.io/login/oauth2/code/naver"
            authorization-grant-type: authorization_code
        provider:
          naver:
            authorization-uri: https://nid.naver.com/oauth2.0/authorize
            token-uri: https://nid.naver.com/oauth2.0/token
            user-info-uri: https://openapi.naver.com/v1/nid/me
            user-name-attribute: response

  websocket:
    enabled: true

  data:
    redis:
      host: ${REDIS_HOST}
      port: 6379
      password: ${REDIS_PASSWORD}

#springdoc:
#  swagger-ui:
#    enabled: true
#    path: /swagger-ui.html
#  api-docs:
#    enabled: true
#    path: /v3/api-doc

openai:
  api:
    key: ${OPENAI_REMOVED}

jwt:
  secret: ${JWT_SECRET_KEY}
  accessToken-expiration: 1800000
  refreshToken-expiration: 1209600000
  oauth-sign-up-expiration: 600000

#cloud:
#  aws:
#    credentials:
#      accessKey: your_access_key #
#      secretKey: your_secret_key #
#    s3:
#      bucket: your_s3_bucket_name
#    region:
#      static: ap-northeast-2

logging:
  level:
    org.hibernate.SQL: debug

file:
  upload-dir: uploads/
